import { supabase } from './supabase/client';

export async function seedInitialBooks() {
  // Função desabilitada - livros agora são adicionados apenas por publicadores ou administradores
  console.log('Seed de livros mockados desabilitado. Livros devem ser adicionados por publicadores ou administradores.');
  return [];
}